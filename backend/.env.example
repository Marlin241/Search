DATABASE_URL=postgresql://user:password@localhost:5432/ats_diagnostic
JWT_SECRET=change-me
ANTHROPIC_API_KEY=sk-ant-...
CORS_ORIGINS=["http://localhost:3000"]
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=personalization
# Emails promus admin automatiquement (séparés par des virgules). Ces
# comptes s'inscrivent sans code d'invitation. Voir scripts/seed_admin.py
# pour créer le compte en une commande. Promotion seule : retirer un email
# ne retire jamais les droits admin.
ADMIN_EMAILS=

FRANCE_TRAVAIL_CLIENT_ID=
FRANCE_TRAVAIL_CLIENT_SECRET=
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
ADZUNA_COUNTRY=fr
LA_BONNE_ALTERNANCE_API_KEY=

# --- Production (beta) — renseigner uniquement dans backend/.env sur le serveur ---
# Généré par: openssl rand -hex 32
# JWT_SECRET=change-me
# Origine autorisée du navigateur (le frontend)
# CORS_ORIGINS=["https://beta.yokkutelabs.com"]
# BACKEND_BASE_URL=https://api.beta.yokkutelabs.com
# FRONTEND_BASE_URL=https://beta.yokkutelabs.com
# Mot de passe Postgres de prod (repris par docker-compose.prod.yml)
# POSTGRES_PASSWORD=change-me
# Identifiants MinIO de prod (repris par docker-compose.prod.yml)
# MINIO_ROOT_USER=change-me
# MINIO_ROOT_PASSWORD=change-me
# Email transactionnel (domaine vérifié dans Resend)
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=no-reply@yokkutelabs.com
# DSN du projet "backend" dans GlitchTip (vide = suivi d'erreurs désactivé)
# GLITCHTIP_DSN=
# ENVIRONMENT=production
# Email qui reçoit les notifications de retours in-app (vide = pas de notif)
# ADMIN_NOTIFY_EMAIL=
# Emails admin (voir plus haut) — à renseigner sur le serveur
# ADMIN_EMAILS=guyroland879@gmail.com
